import BlogList from "../Components/BlogList";
import { Layout } from "../Layout/Layout";

const HomePage = () => {
    return (
        <Layout>
            <BlogList/>
        </Layout>
    );
};

export default HomePage;